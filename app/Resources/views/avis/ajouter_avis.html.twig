{% extends 'base.html.twig' %}

{% block main %}
    <h1>Créez votre compte !</h1>
    <div>
        {{ form_start(form, {attr: {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}

        <button class="btn btn-primary">Ajouter l'avis ! </button>

        {{ form_end(form) }}
    </div>

{% endblock %}

{% block title %}  {%  endblock %}